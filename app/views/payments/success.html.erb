<% content_for :title, "Payment Successful — CUrousell" %>

<div class="max-w-lg mx-auto px-4 py-16">
  <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
    <div class="bg-green-50 border-b border-green-100 px-8 py-10 text-center">
      <div class="w-16 h-16 rounded-full bg-green-100 flex items-center justify-center mx-auto mb-4">
        <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
      </div>
      <h1 class="text-2xl font-bold text-green-700">Payment Submitted</h1>
      <p class="text-green-600 text-sm mt-1">Your payment is being confirmed</p>
    </div>

    <div class="px-8 py-6 space-y-4">
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">Item</span>
        <span class="font-medium text-gray-900 text-right max-w-xs"><%= @order.listing.title %></span>
      </div>
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">Amount paid</span>
        <span class="font-semibold text-gray-900">HK$<%= number_with_precision(@order.amount, precision: 2, delimiter: ",") %></span>
      </div>
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">Order #</span>
        <span class="font-mono text-gray-500"><%= @order.id %></span>
      </div>
      <div class="flex justify-between text-sm">
        <span class="text-gray-500">Status</span>
        <span class="inline-flex items-center gap-1 font-medium
          <%= @order.status == 'paid' ? 'text-green-700' : 'text-amber-600' %>">
          <span class="w-1.5 h-1.5 rounded-full inline-block
            <%= @order.status == 'paid' ? 'bg-green-500' : 'bg-amber-400' %>"></span>
          <%= @order.status == 'paid' ? 'Paid' : 'Processing' %>
        </span>
      </div>
    </div>

    <div class="px-8 pb-8 space-y-3">
      <%= link_to order_path(@order), class: "block text-center bg-cuhk-gold text-cuhk-purple font-semibold py-3 rounded-xl hover:brightness-105 transition-all" do %>
        View Order &amp; Confirm Receipt
      <% end %>
      <%= link_to orders_path, class: "block text-center border border-gray-200 text-gray-500 font-medium py-2.5 rounded-xl hover:bg-gray-50 transition-colors text-sm" do %>
        My Orders
      <% end %>
    </div>
  </div>
</div>
